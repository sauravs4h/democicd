on : push
jobs : 
    first-job:
      runs-on : macos-latest
      steps :
        - run : node --version
        - run : npm --version
        - uses : action/checkout@v3
        - run : node index.js